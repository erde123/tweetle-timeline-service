server:
  port: 8005

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true

spring:
  application:
    name: tweetle-timeline-service
  security:
    oauth2:
      resource server:
        jwt:
          issuer-uri: https://dev-vkjc2qc55bwr1ikt.eu.auth0.com/
          audiences: https://tweetle/api

  data:
    mongodb:
      uri: mongodb://admin:password@localhost:27018/timelinedb?authSource=admin
      database: timelinedb
      username: admin
      password: password
      auto-index-creation: true

  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin

rabbitmq:
  exchange:
    name: tweetle.exchange

auth0:
  domain: ${AUTH0_DOMAIN:dev-vkjc2qc55bwr1ikt.eu.auth0.com}
  clientId: ${AUTH0_CLIENT_ID:7I7v2p9IAvmwm0L3Aunc0NiMZkpAI1aQ}
  clientSecret: ${AUTH0_CLIENT_SECRET:sZkd-oeTWVNFddBMbYISFxCKS0RA-x84IFGxi1-XztYXDALQt3Q_eVQrNfRztM7i}
  audience: ${AUTH0_AUDIENCE:https://tweetle/api}

management:
  endpoints:
    web:
      exposure:
        include: "prometheus"
  endpoint:
    prometheus:
      enabled: true